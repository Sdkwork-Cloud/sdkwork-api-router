# images/kling

## Live Mirror Family

- OpenAPI tag: `images.kling`
- Public path family: `/api/v1/services/aigc/image-generation/generation`
- Shared task lookup: `GET /api/v1/tasks/{task_id}`
- Status: active

## Notes

- SDKWork mirrors the shared DashScope image transport directly.
- Kling and Aliyun image families share the same official upstream path shape while preserving provider identity internally.
