# images/aliyun

## Live Mirror Family

- OpenAPI tag: `images.aliyun`
- Public path family: `/api/v1/services/aigc/image-generation/generation`
- Shared task lookup: `GET /api/v1/tasks/{task_id}`
- Status: active

## Notes

- SDKWork mirrors the shared DashScope image transport directly.
- The public contract stays on Aliyun's official path instead of introducing a gateway wrapper prefix.
