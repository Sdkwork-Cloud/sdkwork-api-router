# 超大文件拆分执行清单

更新时间：2026-04-06

## 目标

- 将交易体系及其直接依赖中的超大文件按高内聚、低耦合原则拆分。
- 单文件优先控制在 300-800 行，超过 1000 行必须进入拆分清单。
- 拆分时优先沿业务边界切分，而不是按“工具函数/常量/组件样式”做碎片化拆分。

## 已完成拆分

- `apps/sdkwork-router-admin/packages/sdkwork-router-admin-commercial/src/index.tsx`
  - 已拆为：
    - `src/index.tsx`
    - `src/formatters.ts`
    - `src/commercialOverviewSections.tsx`
    - `src/commercialOrderAuditDrawer.tsx`
  - 拆分边界：
    - `index.tsx` 负责状态编排和派生数据
    - `commercialOverviewSections.tsx` 负责主工作台区块和表格列定义
    - `commercialOrderAuditDrawer.tsx` 负责订单审计抽屉
    - `formatters.ts` 负责纯格式化与共享展示类型

- `crates/sdkwork-api-interface-admin/src/lib.rs`
  - 已新增：
    - `src/commerce.rs`
  - 拆分边界：
    - `commerce.rs` 负责 commerce 查询参数、审计聚合模型、支付方式管理 handler、订单审计 handler
    - `lib.rs` 保留路由装配、OpenAPI 聚合、共享状态与通用错误映射

- `crates/sdkwork-api-app-commerce/src/lib.rs`
  - 已新增：
    - `src/payment_method.rs`
    - `src/payment_event.rs`
  - 拆分边界：
    - `payment_method.rs` 负责支付方式管理、绑定替换、checkout method 解析
    - `payment_event.rs` 负责支付事件接入、provider 归一化、支付事件幂等落库
    - `lib.rs` 保留订单、优惠券、履约、退款主流程编排

## 当前仍需拆分的超大文件

### P0

- `crates/sdkwork-api-storage-sqlite/src/lib.rs`
  - 当前问题：
    - 存储迁移、mapper、catalog、marketing、commerce、jobs、runtime 全部堆在一个文件
    - 交易链路新增后继续膨胀，已超过 13000 行
  - 目标拆分：
    - `sqlite/migrations.rs`
    - `sqlite/catalog_store.rs`
    - `sqlite/marketing_store.rs`
    - `sqlite/commerce_store.rs`
    - `sqlite/account_store.rs`
    - `sqlite/jobs_store.rs`
    - `sqlite/runtime_store.rs`
    - `sqlite/mappers/*.rs`

- `crates/sdkwork-api-interface-portal/src/lib.rs`
  - 当前问题：
    - portal 身份、营销、commerce、openapi、错误处理混在一起
    - 真实支付接入、webhook、安全验签会继续推高复杂度
  - 目标拆分：
    - `portal/auth.rs`
    - `portal/marketing.rs`
    - `portal/commerce.rs`
    - `portal/webhook.rs`
    - `portal/openapi.rs`

### P1

- `crates/sdkwork-api-interface-admin/src/lib.rs`
  - 下一步继续拆：
    - `admin/catalog.rs`
    - `admin/marketing.rs`
    - `admin/billing.rs`
    - `admin/jobs.rs`
    - `admin/openapi.rs`

- `crates/sdkwork-api-app-commerce/src/lib.rs`
  - 下一步继续拆：
    - `commerce/order.rs`
    - `commerce/catalog.rs`
    - `commerce/refund.rs`
    - `commerce/settlement.rs`
    - `commerce/coupon_integration.rs`

## 命名规范

- Rust 后端文件命名：
  - 使用业务域名词，如 `payment_event.rs`、`commerce.rs`、`refund.rs`
  - 禁止使用 `misc.rs`、`utils.rs`、`temp.rs`、`new.rs`

- React 前端文件命名：
  - 页面主编排：`index.tsx`
  - 复合区块：`<domain><Feature>Sections.tsx`
  - 抽屉/弹窗：`<domain><Feature>Drawer.tsx`、`<domain><Feature>Dialog.tsx`
  - 纯函数：`formatters.ts`、`mappers.ts`

## 拆分原则

- 先抽“成簇”的业务能力，不先抽零散工具函数。
- 先让新模块可以独立理解，再继续收缩原始大文件。
- 模块之间只通过明确的输入输出交互，不让子模块反向持有上层状态。
- 如果一个文件既包含“状态编排”又包含“展示结构”又包含“纯格式化”，必须至少拆成三个层次。


## 2026-04-06 ???????

### ?????

- `crates/sdkwork-api-storage-sqlite/src/lib.rs`
  - ??? `src/commerce_store.rs`
  - ???????????????webhook?????? mapper ? SQLite ????????????
- `crates/sdkwork-api-interface-portal/src/lib.rs`
  - ??? `src/commerce.rs`
  - portal commerce DTO????????checkout/session?payment event?membership handler ? commerce ???????
- `crates/sdkwork-api-app-commerce/src/lib.rs`
  - ??????
    - `src/order.rs`
    - `src/coupon_catalog.rs`
    - `src/coupon_state.rs`
    - `src/settlement.rs`
  - ? `lib.rs` ??? 1000 ???

### ????????

- `crates/sdkwork-api-storage-sqlite/src/lib.rs`
  - ???? `12513` ?
  - ?????? `catalog / marketing / account / jobs / runtime` ??
- `crates/sdkwork-api-interface-admin/src/lib.rs`
  - ???? `5801` ?
  - ?????? `billing.rs / marketing.rs / routing.rs / jobs.rs / openapi.rs`
- `crates/sdkwork-api-interface-portal/src/lib.rs`
  - ???? `4475` ?
  - ?????? `billing.rs / marketing.rs / routing.rs / access.rs`
- `crates/sdkwork-api-storage-postgres/src/lib.rs`
  - ???? `11437` ?
  - ??? sqlite ????????????????

### ?????

- `crates/sdkwork-api-app-commerce/src/lib.rs` ? `828` ?
- `crates/sdkwork-api-app-commerce/src/order.rs` ? `704` ?
- `crates/sdkwork-api-app-commerce/src/coupon_catalog.rs` ? `471` ?
- `crates/sdkwork-api-app-commerce/src/coupon_state.rs` ? `561` ?
- `crates/sdkwork-api-app-commerce/src/settlement.rs` ? `447` ?
- `crates/sdkwork-api-interface-portal/src/commerce.rs` ? `315` ?

- `crates/sdkwork-api-storage-sqlite/src/commerce_store.rs` ???????
  - `src/commerce_checkout_store.rs`
  - `src/commerce_finance_store.rs`
  - `src/commerce_store_mappers.rs`
- `crates/sdkwork-api-storage-sqlite/src/commerce_store.rs` ? `7` ?
- `crates/sdkwork-api-storage-sqlite/src/commerce_checkout_store.rs` ? `683` ?
- `crates/sdkwork-api-storage-sqlite/src/commerce_finance_store.rs` ? `527` ?
- `crates/sdkwork-api-storage-sqlite/src/commerce_store_mappers.rs` ? `279` ?

## 2026-04-06 本轮新增结果（继续拆分）

### 已完成

- `crates/sdkwork-api-interface-portal/src/lib.rs`
  - 新增：`src/auth.rs`、`src/jobs.rs`、`src/marketing.rs`、`src/openapi.rs`、`src/http.rs`
  - 路由层已改为显式依赖子模块：`auth::*`、`marketing::*`、`jobs::*`、`openapi::portal_docs_router()`、`http::*`
  - `lib.rs` 已从 `3253` 行降到 `968` 行
  - 当前保留职责：状态装配、认证提取器、Portal DTO、路由装配、共享账务/工作区辅助逻辑

- `crates/sdkwork-api-storage-postgres/src/lib.rs`
  - 新增：`src/commerce_store.rs`
  - 已抽离：订单持久化、支付事件持久化、项目会员持久化及对应 row mapper
  - `lib.rs` 已从 `10866` 行降到 `10445` 行
  - 当前策略：先沿交易域切开 Postgres commerce 存储，再继续拆 account / marketing / jobs / runtime 等存储职责

### 下一批优先目标

- `crates/sdkwork-api-storage-postgres/src/lib.rs`
  - 继续按领域拆分：`account_store.rs`、`marketing_store.rs`、`jobs_store.rs`、`runtime_store.rs`
  - 目标是对齐 `sqlite` 侧已经形成的 commerce 拆分结构，避免两套存储实现继续漂移

- `crates/sdkwork-api-interface-http/src/lib.rs`
  - 当前仍是全仓最大入口文件，需要按网关入口、认证透传、provider webhook、管理面聚合等边界拆分

- `crates/sdkwork-api-app-gateway/src/lib.rs`
  - 需要按支付/路由/凭据/回调编排拆分，避免交易闭环继续堆在单文件中
- `crates/sdkwork-api-storage-postgres/src/lib.rs`
  - 继续新增：`src/jobs_store.rs`
  - 已继续抽离：async job / attempt / asset / callback 持久化与 decoder
  - `lib.rs` 进一步降到 `10060` 行
- `crates/sdkwork-api-interface-portal/src/marketing.rs`
  - 2026-04-11 update: `marketing.rs` 进一步收敛为薄入口，Portal 营销支持逻辑已拆到 `marketing_support.rs`
  - 继续新增：`src/marketing_handlers.rs`
  - 已继续拆离：Portal 营销 HTTP handler 与营销上下文/预算/幂等支持逻辑
  - `marketing.rs` 已降到 `722` 行，`marketing_handlers.rs` 为 `496` 行

## 2026-04-11 admin marketing split update

- `crates/sdkwork-api-interface-admin/src/marketing.rs`
  - reduced to admin route adapters, request normalization, and error mapping only
  - marketing root now only wires aggregate exports; coupon template, campaign, budget, coupon code, and runtime record handlers live in `src/marketing/template.rs`, `src/marketing/campaign.rs`, `src/marketing/budget.rs`, `src/marketing/code.rs`, and `src/marketing/runtime.rs`, with shared normalization/error translation in `src/marketing/support.rs`
  - coupon template / campaign / budget / coupon code governance orchestration now lives in `crates/sdkwork-api-app-marketing/src/governance.rs`
  - old aggregate-specific admin support modules were removed so interface-admin no longer owns marketing lifecycle state machines
- `crates/sdkwork-api-interface-admin/src/routes.rs`
  - admin route root now delegates the full marketing route surface to `src/routes/marketing_routes.rs`
  - coupon template / campaign / budget / code status and lifecycle endpoints no longer evolve inside the main admin router file
  - admin route root now also delegates commerce route composition to `src/routes/commerce_routes.rs`
  - admin route root now also delegates usage, billing, and pricing route composition to `src/routes/billing_routes.rs`
  - admin route root now also delegates catalog composition to `src/routes/catalog_routes.rs`
  - channel/provider/credential/channel-model/provider-account/provider-model/model-price routes, plus tenant provider readiness, no longer share the same root edit surface as auth/runtime/jobs/routing composition
  - usage summaries, ledger/account reads, pricing lifecycle orchestration, pricing plan mutations, pricing rate mutations, and quota policy endpoints no longer share the same root edit surface as auth/catalog/runtime/jobs/routing composition
  - commerce order audit, payment methods, catalog publication lifecycle, refunds, webhook inbox, and reconciliation routes no longer share the same root edit surface as auth/catalog/billing/routing composition
- `crates/sdkwork-api-interface-admin/src/commerce.rs`
  - commerce root now only wires aggregate exports and shared imports
  - commerce admin surface is split into `src/commerce/order.rs`, `src/commerce/publication.rs`, `src/commerce/payment.rs`, and `src/commerce/reconciliation.rs`, so order audit, governed catalog publication lifecycle, payment method/refund operations, and webhook/reconciliation reads no longer share one 1000+ line handler file
- `crates/sdkwork-api-interface-admin/src/openapi.rs`
  - admin OpenAPI now explicitly documents the marketing `/status` endpoints for coupon template, campaign, budget, and coupon code
  - exposed marketing routes and generated admin docs no longer drift on route coverage
  - admin OpenAPI marketing paths are now split into `src/openapi/marketing_template_paths.rs`, `src/openapi/marketing_campaign_paths.rs`, `src/openapi/marketing_budget_paths.rs`, `src/openapi/marketing_code_paths.rs`, and `src/openapi/marketing_runtime_paths.rs`
  - `openapi.rs` is reduced to OpenAPI assembly and route registration, so marketing path docs evolve on the same aggregate boundaries as admin handlers and route composition
  - non-marketing OpenAPI paths are now also split into `src/openapi/system_paths.rs`, `src/openapi/auth_paths.rs`, `src/openapi/tenant_paths.rs`, `src/openapi/catalog_paths.rs`, `src/openapi/user_paths.rs`, `src/openapi/gateway_paths.rs`, `src/openapi/billing_paths.rs`, and `src/openapi/commerce_paths.rs`
  - system/auth/tenant/catalog/user/gateway/billing/commerce path docs no longer evolve inside one mixed inline module
  - `openapi.rs` is further reduced from roughly `1039` lines to roughly `325` lines
- `crates/sdkwork-api-interface-admin/src/types.rs`
  - admin marketing governance request DTOs are now split into `src/types/marketing.rs`
  - `types.rs` keeps shared admin request/response types while marketing lifecycle payloads evolve beside the marketing admin surface instead of inside one mixed catalog/runtime/types file
  - remaining admin DTOs are now also split into `src/types/auth.rs`, `src/types/billing.rs`, `src/types/catalog.rs`, `src/types/commerce.rs`, `src/types/gateway.rs`, `src/types/pricing.rs`, `src/types/routing.rs`, `src/types/runtime.rs`, `src/types/tenant.rs`, and `src/types/user.rs`
  - auth/session, user admin, catalog/provider/model, commerce publication, gateway, pricing, routing, runtime rollout, and tenant/project DTOs no longer share one mixed root file
  - `types.rs` is reduced from roughly `857` lines to roughly `43` lines while keeping one stable re-export surface
- `crates/sdkwork-api-app-marketing/src/pricing.rs`
  - coupon subsidy and reserve amount calculation now belong to app-marketing
  - app-commerce no longer owns duplicate coupon pricing rules
- `crates/sdkwork-api-interface-admin/src/pricing.rs`
  - pricing root now only wires aggregate exports and shared imports
  - admin pricing surface is split into `src/pricing/lifecycle.rs`, `src/pricing/plan.rs`, `src/pricing/plan_lifecycle.rs`, `src/pricing/rate.rs`, and `src/pricing/quota.rs`, so pricing lifecycle synchronization, plan CRUD/clone, plan publish-schedule-retire transitions, rate CRUD, and quota policy management no longer evolve inside one mixed admin pricing file
  - shared pricing status builders stay exported from the root pricing module, so admin commerce publication flows can keep reusing one canonical pricing status mutation shape without owning billing internals
- `crates/sdkwork-api-interface-admin/src/catalog.rs`
  - catalog root now only wires aggregate exports and shared error translation
  - admin catalog surface is split into `src/catalog/channel.rs`, `src/catalog/provider.rs`, `src/catalog/credential.rs`, `src/catalog/provider_model.rs`, and `src/catalog/model.rs`, so channel registry, provider integration/bindings/readiness, credential + official provider config management, provider-account/provider-model mapping, and canonical model/model-price management evolve independently
  - provider bootstrap syncing for supported models now stays behind the provider-model boundary, while create-provider flows only orchestrate provider creation and delegated model sync
- `crates/sdkwork-api-app-marketing/src/operations.rs`
  - coupon runtime lifecycle is now centralized here: validate, reserve, confirm, release, rollback
  - portal, gateway, and commerce now depend on shared operations instead of rebuilding atomic coupon state transitions in each layer
- `crates/sdkwork-api-app-marketing/src/context.rs`
  - order-scoped marketing context resolution now belongs to app-marketing instead of app-commerce
  - commerce rollback flow now only passes order reference fields and consumes a shared context loader
- `crates/sdkwork-api-app-marketing/src/query.rs`
  - portal marketing subject ownership, redemption listing, code listing, and reward-history read-model assembly now belong to app-marketing instead of interface-portal
  - interface-portal keeps auth, rate-limit, account-arrival enrichment, and DTO mapping only
  - admin canonical marketing list ordering now also lives in shared marketing query helpers, so `interface-admin/src/marketing.rs` keeps only route adaptation and error/status translation
  - admin lifecycle audit list loading is also centralized here, with consistent `requested_at_ms desc` ordering across template/campaign/budget/code audit streams
  - admin commerce order audit now reuses shared marketing evidence loading from app-marketing instead of manually traversing reservation/redemption/code/template/campaign relationships inside `interface-admin/src/commerce.rs`
  - gateway coupon confirm / rollback ownership checks now reuse shared subject-based query helpers instead of duplicating reservation/redemption ownership rules in `interface-http/src/gateway_market.rs`
  - gateway and portal confirm / rollback handlers now also reuse shared ownership+code relation queries, so interface handlers no longer read coupon codes directly just to enforce rate limits
  - gateway marketing subject resolution now uses lightweight account lookup instead of forcing account balance summarization on ownership-only paths
  - commerce coupon catalog listing now reuses shared catalog-visible coupon context assembly from app-marketing instead of scanning marketing storage tables inside `app-commerce/src/coupon_catalog.rs`
  - commerce coupon rollback now reuses shared marketing evidence loading for redemption/reservation lookup instead of querying marketing relation records directly inside `app-commerce/src/coupon_state.rs`
- `crates/sdkwork-api-app-marketing/src/operations.rs`
  - runtime subject-ownership validation now reuses shared query helpers while preserving `not_found` vs `forbidden` semantics for external API behavior
  - operations root now only wires exports; validate, reserve, confirm, release, rollback, replay, error/types, and subject-ownership helpers are split into `src/operations/validate.rs`, `src/operations/reserve.rs`, `src/operations/confirm.rs`, `src/operations/release.rs`, `src/operations/rollback.rs`, `src/operations/replay.rs`, `src/operations/error.rs`, `src/operations/types.rs`, and `src/operations/support.rs`
  - reserve/confirm/rollback idempotent replay paths are further isolated into `src/operations/replay/reserve.rs`, `src/operations/replay/confirm.rs`, and `src/operations/replay/rollback.rs`
  - idempotent replay checks no longer evolve in the same leaf files as fresh request validation and atomic mutation orchestration
- `crates/sdkwork-api-app-marketing/src/governance.rs`
  - admin governance now shares one app-layer implementation for coupon template, marketing campaign, campaign budget, and coupon code lifecycle actions
  - clone collisions are modeled as explicit `Conflict` outcomes so admin APIs can return `409` for duplicate governed revisions instead of collapsing everything into `400`
  - governance root now only owns shared errors, timestamp/name helpers, and export wiring; aggregate-specific lifecycle rules live in `src/governance/template.rs`, `src/governance/campaign.rs`, `src/governance/budget.rs`, and `src/governance/code.rs`
  - coupon template, campaign, budget, and coupon code audit construction no longer share one 1400+ line file, which reduces cross-aggregate change coupling and makes follow-up rule tightening safer
- `crates/sdkwork-api-app-marketing/src/governance/template.rs`
  - template governance root now only wires exports; action/detail types, lookup/lineage helpers, actionability/detail projection, audit construction/persistence, lifecycle mutation, and revision comparison are split into `src/governance/template/types.rs`, `src/governance/template/lookup.rs`, `src/governance/template/actionability.rs`, `src/governance/template/audit.rs`, `src/governance/template/mutation.rs`, and `src/governance/template/comparison.rs`
  - clone flow, lifecycle mutation flow, and revision diff logic no longer evolve in the same leaf file
- `crates/sdkwork-api-app-marketing/src/governance/campaign.rs`
  - campaign governance root now only wires exports; action/detail types, lookup/lineage helpers, actionability/detail projection, audit construction/persistence, lifecycle mutation, and revision comparison are split into `src/governance/campaign/types.rs`, `src/governance/campaign/lookup.rs`, `src/governance/campaign/actionability.rs`, `src/governance/campaign/audit.rs`, `src/governance/campaign/mutation.rs`, and `src/governance/campaign/comparison.rs`
  - campaign lifecycle rules, clone lineage handling, and revision diff assembly now have independent edit surfaces
- `crates/sdkwork-api-app-marketing/src/governance/budget.rs`
  - budget governance root now only wires exports; action/detail types, actionability/detail projection, lookup helpers, audit persistence, and lifecycle mutation are split into `src/governance/budget/types.rs`, `src/governance/budget/actionability.rs`, `src/governance/budget/lookup.rs`, `src/governance/budget/audit.rs`, and `src/governance/budget/mutation.rs`
  - budget lifecycle rule calculation, lookup loading, and audit persistence no longer share one mixed helper file
- `crates/sdkwork-api-app-marketing/src/governance/code.rs`
  - coupon-code governance root now only wires exports; action/detail types, actionability/detail projection, lookup helpers, audit persistence, and lifecycle mutation are split into `src/governance/code/types.rs`, `src/governance/code/actionability.rs`, `src/governance/code/lookup.rs`, `src/governance/code/audit.rs`, and `src/governance/code/mutation.rs`
  - coupon-code expiration/actionability rules, lookup loading, and lifecycle audit flow now have independent edit surfaces
- `crates/sdkwork-api-app-marketing/src/create.rs`
  - admin marketing create flows now also terminate in app-marketing instead of inserting storage records directly from `interface-admin`
  - create orchestration now owns field normalization, relation existence checks, and business-key conflict detection for template key, campaign id, budget id, coupon code id, and normalized coupon code value
- `crates/sdkwork-api-app-marketing/src/recovery.rs`
  - expired coupon reservation recovery is now owned by app-marketing instead of app-jobs
  - recovery orchestration now keeps coupon code release, budget release, reservation expiration, and marketing outbox emission inside one shared marketing kernel
- `crates/sdkwork-api-app-marketing/src/context.rs`
  - coupon code normalization is now the single shared source for marketing runtime, bootstrap-data import, and coupon-specific rate limiting
  - app-runtime and app-rate-limit no longer carry divergent coupon-code canonicalization rules
  - context root now only wires exports; coupon-context types, context resolution, catalog visibility/inventory logic, and expired reservation recovery are split into `src/context/types.rs`, `src/context/resolution.rs`, `src/context/visibility.rs`, and `src/context/recovery.rs`
  - context loading and expired-reservation release logic no longer evolve in the same file
- `crates/sdkwork-api-app-marketing/src/query.rs`
  - catalog-visible coupon display projection now also belongs to app-marketing instead of `app-commerce`
  - coupon list presentation fields such as `discount_label / audience / note / expires_on / remaining` now derive from one shared marketing read model
  - coupon resolution by catalog code now also terminates in app-marketing, so commerce no longer performs its own catalog-visible filtering before building a commercial coupon definition
  - query root now only owns export wiring; subject ownership/history, admin list reads, order evidence assembly, and catalog projection are split into `src/query/subject.rs`, `src/query/admin.rs`, `src/query/order_evidence.rs`, and `src/query/catalog.rs`
  - catalog and ownership read models no longer share one large file, reducing cross-surface regression risk when portal/admin/catalog requirements evolve independently
- `crates/sdkwork-api-app-marketing/src/query/admin.rs`
  - admin query root now only wires exports; canonical entity list reads and lifecycle audit reads live in `src/query/admin/lists.rs` and `src/query/admin/audits.rs`
  - admin list ordering and audit ordering no longer evolve in one mixed admin query leaf
- `crates/sdkwork-api-app-marketing/src/query/subject.rs`
  - subject query root now only wires exports; ownership checks, reward/code history assembly, and internal record loaders are split into `src/query/subject/ownership.rs`, `src/query/subject/history.rs`, `src/query/subject/support.rs`, and `src/query/subject/types.rs`
  - portal-facing subject ownership checks no longer change in the same file as reward-history aggregation and summary projection logic
- `crates/sdkwork-api-app-runtime/src/bootstrap_data/registry.rs`
  - bootstrap marketing stages now use shared `ensure_*` orchestration from app-marketing instead of raw inserts
  - repeated initialization of the same canonical marketing template/campaign/budget/code records is now treated as idempotent no-op, while divergent duplicates fail explicitly
- `crates/sdkwork-api-app-marketing/src/create.rs`
  - admin create flows and bootstrap `ensure_*` flows now share one aggregate-specific persist core instead of maintaining duplicated create/ensure branches
  - primary-id conflicts, business-key conflicts, relation checks, and coupon-template/code compatibility rules now evolve in one place
  - create root now only wires exports; template, campaign, budget, and coupon code create/ensure flows are split into `src/create/template.rs`, `src/create/campaign.rs`, `src/create/budget.rs`, `src/create/code.rs`, with shared normalize/conflict helpers in `src/create/support.rs`
  - bootstrap idempotent ensure logic no longer changes in the same file as all aggregate-specific validation rules
- `crates/sdkwork-api-app-marketing/src/create/template.rs`
- `crates/sdkwork-api-app-marketing/src/create/campaign.rs`
- `crates/sdkwork-api-app-marketing/src/create/budget.rs`
- `crates/sdkwork-api-app-marketing/src/create/code.rs`
  - aggregate create roots now only wire exports; each aggregate splits create-time normalization into `prepare.rs` and idempotent insert/conflict handling into `persist.rs`
  - aggregate create validation and persistence conflict handling no longer share one mixed leaf file
- `crates/sdkwork-api-app-jobs/src/lib.rs`
  - jobs now wraps shared marketing recovery and records metrics only
  - app-jobs no longer carries direct coupon recovery state transitions or marketing transaction internals
- `crates/sdkwork-api-interface-admin/src/marketing.rs`
  - admin marketing create handlers now mirror lifecycle handlers: request binding plus HTTP status translation only
  - interface-admin no longer performs direct canonical template/campaign/budget/code inserts for marketing
- `crates/sdkwork-api-interface-admin/src/marketing/support.rs`
  - admin marketing template/campaign/budget/code status updates now resolve governed records through focused `find_*` store methods instead of binding handlers to list-scan flows
  - admin status mutation paths now depend on stable aggregate lookup interfaces, which keeps future storage-side indexing and query specialization behind the store boundary
- `crates/sdkwork-api-storage-core/src/admin_store.rs`
- `crates/sdkwork-api-storage-core/src/marketing_store.rs`
  - added default focused marketing lookup helpers for campaign/budget/rollback relations instead of forcing app-layer code to repeatedly scan full collections
  - added default lineage and code-bound lookup helpers for template/campaign revisions and coupon reservations, reducing app-layer dependence on full-table scans
  - added subject/id relation helpers plus empty-input short-circuiting for code, redemption, and rollback batch lookups so shared query assembly does not accidentally fall back to full-table scans on empty sets
  - default coupon-code lookup by value now normalizes both query input and stored values, so fallback/in-memory implementations cannot drift on casing or whitespace
- `crates/sdkwork-api-storage-postgres/src/marketing_store_impl.rs`
- `crates/sdkwork-api-storage-sqlite/src/marketing_store_impl.rs`
  - postgres/sqlite marketing stores now implement focused point lookups for canonical marketing campaign and campaign budget records, instead of forcing admin status mutations through full canonical list queries
- `crates/sdkwork-api-app-marketing/src/context.rs`
  - shared coupon context resolution now falls back to the latest campaign revision when no campaign is currently effective
  - historical coupon reads, rollback flows, and idempotent replay no longer lose campaign context only because rollout time windows have ended
- `crates/sdkwork-api-domain-marketing/src/lib.rs`
  - coupon redemption records now separate `budget_consumed_minor` from `subsidy_amount_minor`
  - this removes the previous semantic coupling that broke grant-unit coupon budget settlement
  - coupon code normalization is now owned by the domain layer and re-exported by `app-marketing`, so runtime, bootstrap, rate-limit, and storage all share the same canonical value rule
- `crates/sdkwork-api-storage-sqlite/src/sqlite_migration_marketing_schema.rs`
- `crates/sdkwork-api-storage-postgres/src/postgres_migration_marketing_schema.rs`
  - budget/reservation/redemption/rollback finance fields are now explicit schema columns, not only embedded inside `record_json`
- `crates/sdkwork-api-app-marketing/src/lib.rs`
  - crate root is now assembly-only, and the pure coupon validation/reserve/confirm/rollback kernel moved into `src/kernel.rs`
  - export wiring and runtime kernel rules no longer share the same root file
- `crates/sdkwork-api-app-marketing/src/governance/template.rs`
  - template governance internals are now refined from one mixed `support.rs` leaf into `lookup.rs`, `actionability.rs`, and `audit.rs`
  - template lineage/loading, actionability/detail projection, and audit persistence can now evolve independently without helper-surface drift
- `crates/sdkwork-api-app-marketing/src/governance/campaign.rs`
  - campaign governance internals are now refined from one mixed `support.rs` leaf into `lookup.rs`, `actionability.rs`, and `audit.rs`
  - campaign lineage/loading, lifecycle decisioning, and audit construction no longer share one helper file
- `crates/sdkwork-api-app-marketing/src/recovery.rs`
  - recovery root now only orchestrates reservation expiration, while budget release planning, expiration rules, outbox payload construction, and report types live in `src/recovery/budget.rs`, `src/recovery/expiration.rs`, `src/recovery/outbox.rs`, and `src/recovery/types.rs`
  - future recovery-policy changes can now tighten budget, outbox, and expiration semantics independently
  - recovery root now only wires exports; reservation scan orchestration and single-reservation transactional recovery now live in `src/recovery/runner.rs` and `src/recovery/reservation.rs`
  - recovery scan loop and transactional expiration mutation no longer share one leaf file
- `crates/sdkwork-api-app-marketing/src/query/subject/history.rs`
  - subject history root now only wires exports; subject redemption listing, code-view assembly, reward-history assembly, and summary projection live in `src/query/subject/history/redemptions.rs`, `src/query/subject/history/code_views.rs`, `src/query/subject/history/reward_history.rs`, and `src/query/subject/history/summary.rs`
  - portal-facing subject history read models no longer evolve in one mixed file
- `crates/sdkwork-api-app-marketing/src/governance/template/mutation.rs`
  - template mutation root now only wires exports; lifecycle transitions and clone revision orchestration live in `src/governance/template/mutation/lifecycle.rs` and `src/governance/template/mutation/clone.rs`
  - template lifecycle root now only wires exports; lifecycle mutation orchestration, rejection audit guardrails, and transition table rules live in `src/governance/template/mutation/lifecycle/apply.rs`, `src/governance/template/mutation/lifecycle/validation.rs`, and `src/governance/template/mutation/lifecycle/transition.rs`
  - template publish/schedule/retire flow no longer evolves in the same leaf file as clone identity and revision fork rules
- `crates/sdkwork-api-app-marketing/src/governance/campaign/mutation.rs`
  - campaign mutation root now only wires exports; lifecycle transitions and clone revision orchestration live in `src/governance/campaign/mutation/lifecycle.rs` and `src/governance/campaign/mutation/clone.rs`
  - campaign lifecycle root now only wires exports; lifecycle mutation orchestration, rejection audit guardrails, and transition table rules live in `src/governance/campaign/mutation/lifecycle/apply.rs`, `src/governance/campaign/mutation/lifecycle/validation.rs`, and `src/governance/campaign/mutation/lifecycle/transition.rs`
  - campaign publish/schedule/retire flow no longer evolves in the same leaf file as clone lineage and display-name fork rules
- `crates/sdkwork-api-app-marketing/src/kernel.rs`
  - coupon kernel root now only wires exports; validation decision, service error, validation, reserve, confirm, and rollback rules live in `src/kernel/decision.rs`, `src/kernel/error.rs`, `src/kernel/validate.rs`, `src/kernel/reserve.rs`, `src/kernel/confirm.rs`, and `src/kernel/rollback.rs`
  - pure coupon validation and transition rules no longer evolve in one mixed kernel file
- `crates/sdkwork-api-app-marketing/src/state.rs`
  - marketing state root now only wires exports; coupon-code transitions and campaign-budget transitions live in `src/state/code.rs` and `src/state/budget.rs`, with shared transition helpers in `src/state/support.rs`
  - code-status mutation and budget-status mutation no longer share one mixed state file
- `crates/sdkwork-api-app-marketing/src/idempotency.rs`
  - idempotency root now only wires exports; error types, key normalization, fingerprinting, and coupon reservation/redemption/rollback ID derivation live in `src/idempotency/errors.rs`, `src/idempotency/keys.rs`, `src/idempotency/fingerprint.rs`, and `src/idempotency/ids.rs`
  - idempotency validation and business ID derivation no longer evolve in one mixed runtime helper file
- `crates/sdkwork-api-app-marketing/src/context/resolution.rs`
  - context resolution root now only wires exports; reference-based coupon-code resolution, campaign/budget selection, and storage-backed context loading live in `src/context/resolution/reference.rs`, `src/context/resolution/selection.rs`, and `src/context/resolution/loaders.rs`
  - reference parsing, campaign fallback policy, and storage joins no longer evolve inside one mixed context leaf
- `crates/sdkwork-api-app-marketing/src/query/catalog.rs`
  - catalog root now only wires exports; catalog view types, formatting/projection, and storage-backed visibility loading live in `src/query/catalog/types.rs`, `src/query/catalog/formatting.rs`, and `src/query/catalog/loaders.rs`
  - discount label formatting and catalog context loading no longer evolve in one mixed catalog file
- `crates/sdkwork-api-app-marketing/src/query/subject/support.rs`
  - subject support root now only wires internal re-exports; subject-scoped record fan-out and latest-by-code aggregation live in `src/query/subject/support/loaders.rs` and `src/query/subject/support/aggregation.rs`
  - subject lookup fan-out and latest reservation/redemption reduction no longer share one mixed helper file
- `crates/sdkwork-api-app-marketing/src/create/support.rs`
  - create support root now only wires shared create helpers; persist-mode semantics, input normalization, error mapping, and relation/template compatibility checks live in `src/create/support/mode.rs`, `src/create/support/normalize.rs`, `src/create/support/errors.rs`, and `src/create/support/relations.rs`
  - create-time idempotency policy, field normalization, and relation validation no longer share one mixed helper file
- `crates/sdkwork-api-app-marketing/src/query/order_evidence.rs`
  - order-evidence root now only wires exports; evidence view types, assembly, coupon evidence loading, and campaign evidence loading live in `src/query/order_evidence/types.rs`, `src/query/order_evidence/loaders.rs`, `src/query/order_evidence/coupon.rs`, and `src/query/order_evidence/campaign.rs`
  - order evidence assembly no longer shares one leaf file with coupon relation loading and campaign lookup logic
- `crates/sdkwork-api-app-marketing/src/governance/template/actionability.rs`
  - template actionability root now only wires internal exports; rollout action decisioning and detail projection live in `src/governance/template/actionability/decision.rs` and `src/governance/template/actionability/detail.rs`
  - template action permission rules and detail assembly no longer share one leaf file
- `crates/sdkwork-api-app-marketing/src/governance/campaign/actionability.rs`
  - campaign actionability root now only wires internal exports; rollout action decisioning and detail projection live in `src/governance/campaign/actionability/decision.rs` and `src/governance/campaign/actionability/detail.rs`
  - campaign action permission rules and detail assembly no longer share one leaf file
