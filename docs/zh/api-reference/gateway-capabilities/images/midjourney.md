# images/midjourney

## 发布状态

- 状态：未发布能力说明
- 真实镜像家族：无
- 公开路径家族：无

## 为什么没有发布

- SDKWork 的镜像规则要求 provider 先有一个“只切 `base_url` 就能镜像”的官方 API 面。
- Midjourney 当前没有满足这一规则的公开官方 API 契约。
- 因此当前不会为了目录对称性而额外发明 `images.midjourney` 路由或 tag。
