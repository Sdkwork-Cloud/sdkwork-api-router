# Keep this file available for future Cargo workspace tuning, but do not pin
# Windows-only CMake generator defaults here.
#
# Cargo loads .cargo/config.toml on every host platform, so global CMAKE_GENERATOR
# settings leak into Linux and macOS release lanes. Windows-safe generator
# normalization belongs in the Windows entrypoints and CI wiring instead.
